/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['clous.s3.eu-west-3.amazonaws.com'], // Add your S3 domain here
    },
  };
  
  export default nextConfig;
  